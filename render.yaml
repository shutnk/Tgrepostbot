services:
  - type: web
    name: telegram-forward-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: SESSION_B64
        sync: false
      - key: PORT
        value: 10000
